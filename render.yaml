services:
  - type: web
    name: qr-scan-tracker
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn Counter:app --host 0.0.0.0 --port $PORT
    envVars:
      PUBLIC_BASE_URL: ${RENDER_EXTERNAL_URL}
    healthCheckPath: /healthz